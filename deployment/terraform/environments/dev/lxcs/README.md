# environments/dev/lxcs

_Stub — not yet implemented._

Will provision development LXC containers using `modules/lxc`. 
Same file structure as `dev/vms/`: `providers.tf`, `backend.tf`, `locals.tf`, `variables.tf`, `main.tf`, `outputs.tf`, `terraform.tfvars`, `secrets.auto.tfvars`.
