# environments/prod/vms

_Stub — not yet implemented._

Same structure as `environments/dev/vms/` but with production-scale specs in `locals.tf`. Apply after `global/`, `prod/dns/`, and `prod/firewall/`.
