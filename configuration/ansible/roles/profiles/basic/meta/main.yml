---
galaxy_info:
  author: tobi
  description: Foundation profile — applied to every host
  license: UNLICENSED
  min_ansible_version: "2.9"

dependencies:
  - role: features/snap_removal   # must run before base_packages (adds Mozilla apt repo)
  - role: features/base_packages
